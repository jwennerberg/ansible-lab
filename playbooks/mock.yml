- name: Noop playbook
  hosts: all
  gather_facts: false
  tasks:
    - name: Mock message
      ansible.builtin.debug:
        msg: "{{ prefix | default('DEBUG') }}: {{ mock_message | default('mock') }}"
